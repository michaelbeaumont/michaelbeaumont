export interface Language {
  name: string;
  count: number;
  color: string;
}
export interface Topic {
  name: string;
  url: string;
}
export interface Org {
  login: string;
  url: string;
  avatarUrl: string;
}
